# دليل تثبيت واستخدام نظام ULTRA_POS (الكاشير الاحترافي)

أهلاً بك في نظام **ULTRA_POS**. يوضح هذا الدليل خطوات التثبيت، التشغيل، وتفعيل البرنامج باستخدام مولد المفاتيح الخاص بك.

---

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من وجود ما يلي على جهازك:
1.  **Node.js**: (الإصدار 16 أو أحدث).
2.  **Git**: (اختياري، لتحميل الكود).
3.  **صلاحيات المسؤول (Administrator)**: لتشغيل أوامر التثبيت دون مشاكل.

---

## 🛠️ خطوات التثبيت والتشغيل

### الخطوة 1: تثبيت المكتبات (Dependencies)
افتح موجه الأوامر (CMD) أو PowerShell في مجلد المشروع ونفذ الأمر التالي:

```bash
npm install
```
*هذه الخطوة قد تستغرق بضع دقائق لتحميل جميع الملفات المطلوبة.*

### الخطوة 2: تشغيل البرنامج (وضع التطوير)
لتجربة البرنامج والتأكد من عمله، استخدم الأمر:

```bash
npm start
```
سيفتح البرنامج واجهة الدخول أو شاشة التفعيل.

---

## 🔑 خطوات تفعيل البرنامج

نظام ULTRA_POS محمي بنظام ترخيص يتطلب تفعيلًا خاصًا. نظرًا لأنك المالك، يمكنك توليد مفاتيح التفعيل بنفسك باستخدام الأداة المرفقة.

### 1. الحصول على "رمز الجهاز" (Device Hash)
1.  قم بتشغيل تطبيق **ULTRA_POS**.
2.  إذا لم يكن البرنامج مفعلاً، ستظهر لك شاشة التفعيل تلقائياً.
3.  في شاشة التفعيل، ستجد حقلاً بعنوان **"رمز جهازك (Device Hash)"**.
4.  اضغط على زر النسخ 📋 بجانب الرمز لنسخه.

### 2. توليد مفتاح التفعيل (بواسطة الأداة الخاصة)
1.  اذهب إلى مجلد المشروع الرئيسي.
2.  افتح الملف المسمى `keygen.html` باستخدام أي متصفح (مثل Chrome أو Edge).
3.  ستظهر لك واجهة **"Générateur de Licence ULTRA_POS"**.
4.  الصق **رمز الجهاز** الذي نسخته في الخطوة السابقة في خانة **Device Hash**.
5.  حدد **تاريخ الانتهاء** (Expiry Date) الذي تريده للترخيص.
6.  اضغط على زر **"Générer la Clé"**.
7.  سيظهر لك كود طويل في المربع في الأسفل. اضغط **"Copier la clé"**.

### 3. إدخال المفتاح وتفعيل البرنامج
1.  عد إلى تطبيق **ULTRA_POS**.
2.  الصق الكود الطويل في خانة **"أدخل كود التفعيل هنا"**.
3.  اضغط على زر **"✓ تفعيل الآن"**.
4.  سيتم تفعيل البرنامج وفتح الواجهة الرئيسية فوراً.

---

## 🏗️ إنشاء ملفات التثبيت (Setup.exe)

لإنشاء ملفات تثبيت احترافية للبرنامج ومولد المفاتيح:

1.  افتح PowerShell كمسؤول (Run as Administrator).
2.  نفذ السكريبت الشامل:
    ```powershell
    .\build_all.ps1
    ```
3.  سيقوم السكريبت ببناء البرنامجين تلقائياً.
4.  عند الانتهاء، سيفتح مجلد `dist-final` الحاوي على:
    *   `ULTRA_POS_Setup.exe`: ملف تثبيت البرنامج الرئيسي.
    *   `ULTRA_POS_Keygen_Setup.exe`: ملف تثبيت مولد المفاتيح (Activatot).

---

## 💡 ملاحظات هامة

*   **مولد المفاتيح (Keygen App)**: أصبح الآن برنامجاً مستقلاً (`ULTRA_POS_Keygen_Setup.exe`). يمكنك تثبيته على جهازك الخاص لتوليد المفاتيح للعملاء دون الحاجة لمتصفح.
*   **قاعدة البيانات**: البرنامج ينشئ قاعدة بيانات محلية ولا يحتاج لاتصال دائم بالإنترنت.

بالتوفيق في مشروعك الجديد **ULTRA_POS**! 🚀
